Испытания проводятся с последней версией allpy из репозитория
http://kodomo.fbb.msu.ru/hg/allpy

Вместо имен программ используются относительные пути к локальным файлам.

Веб-интерфейс:
 * переименован файл test_corr1.homology в test_corr1.hom
 * ослаблено требование к реагированию на неверный вормат входных данных
   (допускается выдача пустых файлов)

Pymol:
 * остатки блоков показываются все вместе,
   хотя отделены визуально от остальных остатков
 * p1.fasta -> set1.fasta, ...
 * опция -p -> --high-blocks-pymol

Pair-cores:
 * вместо опции -H используется опция --high-blocks-html

